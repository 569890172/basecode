import React, { PureComponent } from 'react'
import { ScrollView } from 'react-native'

export class baseUI extends PureComponent {
    render() {
        return (
            <ScrollView style={{width:'100%',height:'100%',backgroundColor:'yellow'}} >
                
            </ScrollView>
        )
    }
}
